extends user

block response

  ul
    each action in actions
      li
        a(href='/users/#{index}/#{action.path}/#{systemId}?#{action.query}').link= action.name

  hr
  h3= api

  table
    each value, index in data
      tr
        td: strong= index
        td: pre= JSON.stringify(value, null, 3)